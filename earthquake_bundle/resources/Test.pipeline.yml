resources:
  pipelines:
    Test:
      name: Bronze_Silver_prod1
      configuration:
        catalog_name: ${var.catalog}
      libraries:
        - glob:
            include: ../src/Bronze_to_Silver/transformations/**
      schema: silver
      continuous: false
      development: false
      photon: true
      channel: CURRENT
      catalog: ${var.catalog}
      serverless: true
      root_path: ../src/Bronze_to_Silver




